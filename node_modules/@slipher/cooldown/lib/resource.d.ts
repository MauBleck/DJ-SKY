import { BaseResource, type CacheFrom } from 'seyfert/lib/cache';
import type { PickPartial } from 'seyfert/lib/common';
export interface CooldownData {
    remaining: number;
    interval: number;
    lastDrip: number;
}
export declare enum CooldownType {
    User = "user",
    Guild = "guild",
    Channel = "channel"
}
export declare class CooldownResource extends BaseResource<CooldownData> {
    namespace: string;
    filter(_data: CooldownData, _id: string): boolean;
    set(from: CacheFrom, id: string, data: PickPartial<CooldownData, 'lastDrip'>): import("seyfert/lib/common").Awaitable<void>;
}
